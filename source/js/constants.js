const programsButton = document.querySelectorAll('.programs__button');
const commonButtonControls = document.querySelectorAll('.common-control__button');
const mainNav = document.querySelector('.nav');
const toggle = document.querySelector('.toggle');
const toggleIcon = document.querySelector('.toggle__icon');
const iconOpen = 'toggle__icon--open';
const iconClose = 'toggle__icon--close';

export {programsButton, commonButtonControls, mainNav, toggle, toggleIcon, iconOpen, iconClose};
